[pytest]
pythonpath = .
testpaths = tests
markers =
    integration: loads the real embedding/cross-encoder models and the production index (slower, no network calls but real disk/CPU work) — run everything but these with `-m "not integration"`.
